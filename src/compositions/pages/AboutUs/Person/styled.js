import styled from 'styled-components';
import { spacing } from 'styles/spacing';
import { Copy } from 'components/Typography';

export const Img = styled.img`
  border-radius: 50%;
  width: 180px;
  width: 180px;
`;

export const Title = styled(Copy)`
  margin-top: ${spacing(-2)};
`;
