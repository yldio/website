import styled from 'styled-components';
import PageRule from 'components/PageRule';
import { spacing } from 'styles/spacing';

export const Rule = styled(PageRule)`
  margin-bottom: ${spacing(8)};
`;
