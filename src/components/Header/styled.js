import styled from 'styled-components';
import { gridColWidth } from 'styles/spacing';

export const Wrapper = styled.header`
  display: flex;
  justify-content: space-between;
  margin: 30px ${gridColWidth / 2}px;
  align-items: center;
`;
