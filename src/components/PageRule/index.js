import styled from 'styled-components';

import { spacing } from 'styles/spacing';
import Rule from 'components/Rule';

const PageRule = styled(Rule)`
  margin: ${spacing(10)} 0;
`;

export default PageRule;
